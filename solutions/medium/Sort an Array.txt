// Title: Sort an Array
            // Difficulty: Medium
            // Language: Python3
            // Link: https://leetcode.com/problems/sort-an-array/

                if listy[i]<=listy[j]:
                    temp.append(listy[i])
                    i+=1
                elif listy[i]>=listy[j]:
                    temp.append(listy[j])
                    j+=1
            while i<=mid:
                temp.append(listy[i])
                i += 1
            while j<=end:
                temp.append(listy[j])
                j += 1

            for i in range(len(temp)):
                listy[i+start] = temp[i]
            return listy
    
        def mergesort(listy,start,end):
            if start<end:
                mid = start + int((end - start) / 2)
                mergesort(listy,start,mid)
                mergesort(listy,mid+1,end)
                merge(listy,start,mid,end)
            return listy

        
        end = len(nums)-1
        mergesort(nums, 0, end)
        return nums
