// Title: Valid Parentheses
            // Difficulty: Easy
            // Language: Python
            // Link: https://leetcode.com/problems/valid-parentheses/

        for char in s:
            if char in mapping:
                # Pop top element if stack is 
                non-empty; else assign dummy value

        mapping = {')': '(', '}': '{', ']': '['}
        stack = []

            return False
        if len(s) % 2 != 0:
        can never be balanced
        # Quick edge case check: odd length strings 
        """
        :rtype: bool
        :type s: str
        """
    def isValid(self, s):
class Solution(object):
